function setup() {
  createCanvas(800, 600);
  background(30);
  fill(255); textAlign(CENTER, CENTER); textSize(24);
  text('p5 loaded ✓  (click once to unlock audio)', width/2, height/2);
}
function draw() {}


